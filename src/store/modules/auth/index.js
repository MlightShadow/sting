const state = {
  token: localStorage.getItem('token') || null,
  me: {},
};

const getters = {};

const actions = {

};

const mutations = {

};

export default {
  state,
  getters,
  actions,
  mutations,
};
